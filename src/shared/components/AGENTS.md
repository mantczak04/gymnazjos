# Shared Components

This directory contains app-wide reusable components.

- Keep components generic and domain-neutral.
- App navigation and password gate live here because they frame the whole app.
- UI primitives belong in `ui`.
- Avoid product-specific Supabase queries in shared components.
