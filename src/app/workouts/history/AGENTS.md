# Workout History Routes

These routes render completed workout history and saved session details.

- History must show completed sessions only.
- Discarded active drafts must never appear here.
- Saved session editing should reuse the same set semantics as active workout logging.
- Keep route files thin and place behavior in `src/features/workouts/components`.
