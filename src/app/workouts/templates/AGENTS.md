# Workout Templates Route

This route renders template management.

- Delegate behavior to `WorkoutTemplatesPage`.
- Templates contain ordered exercise lists only.
- Use Move up / Move down controls; do not add drag-and-drop in the MVP.
- Deactivate historical templates instead of hard deleting them.
