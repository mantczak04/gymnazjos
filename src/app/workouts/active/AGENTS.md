# Active Workout Route

This route renders the active workout logging screen.

- Delegate behavior to `ActiveWorkoutPage`.
- Active draft persistence is owned by `useActiveWorkoutDraft`.
- Do not create exercises from this route; ad-hoc additions must use existing Exercise Library records.
