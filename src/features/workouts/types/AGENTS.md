# Workout Types

This directory contains workout domain TypeScript types.

- Use camelCase domain types, even when Supabase rows are snake_case.
- Keep public domain shapes stable and small.
- Do not duplicate Supabase generated row types here.
- Add new types only when they clarify feature boundaries.
