# Workout Schemas

This directory contains Zod schemas for workout forms and data boundaries.

- Use schemas for form validation, localStorage draft validation, and save-flow validation.
- Keep schema names explicit and domain-specific.
- Update tests when validation rules change.
- Do not rely on database constraints alone for user-facing validation.
