# Workout Components

This directory contains screen-level and feature-specific React components for workouts.

- Components may compose UI, forms, hooks, and query mutations.
- Keep business calculations in `utils` and database operations in `queries`.
- Keep mobile data entry fast: direct controls, lightweight forms, minimal navigation.
- Use shared UI primitives from `src/shared/components/ui`.
