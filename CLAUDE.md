# Claude Instructions

Follow `AGENTS.md`. Prefer explicit TypeScript, feature-local modules, and small changes that preserve the documented workout MVP behavior.

Before editing, read the root `AGENTS.md` and every nested `AGENTS.md` that applies to the files being touched. More specific nested files override broader guidance for their subtree. If a task spans multiple subtrees, follow each subtree's local instructions.
